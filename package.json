{
  "name": "nectenda-plugin",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "pnpm -r build && node -e \"require('fs').copyFileSync('packages/plugin/main.js','main.js')\"",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
